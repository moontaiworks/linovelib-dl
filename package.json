{
  "name": "linovellib-dl",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "bin": {
    "linovellib-dl": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json && node --test"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "yazl": "^3.3.1"
  }
}
