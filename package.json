{
  "name": "linovel-dl",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json && node --test"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "cheerio": "^1.2.0"
  }
}
